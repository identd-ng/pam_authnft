# llvm-cov HTML output is regenerated by `make fuzz-coverage`. The
# directory persists across `make clean` (so a developer can browse
# the last report without rebuilding) but the HTML files themselves
# are not committed — they would churn on every coverage update and
# bloat the repo. Run `make fuzz-coverage` to refresh.
*
!.gitignore
!README.md
