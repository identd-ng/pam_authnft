pam_authnft TODO
================

Near-term
---------
[ ] Publish pam_authnft(8) manpage via `make install`
[ ] Dependabot + CodeQL rollout validation
[x] cppcheck workflow
[x] ASan/UBSan build target for CI
[x] GCC + Clang build matrix in CI
[x] Coverity Scan weekly cron
[x] Codecov coverage upload
[x] OpenSSF Scorecard
[ ] CIFuzz / OSS-Fuzz integration — write libFuzzer harness
    (starting with util_is_valid_username and nft rule parsing),
    apply for OSS-Fuzz onboarding, wire in the CIFuzz action for
    per-PR short fuzz runs
[ ] Integration test for IPv6 path (currently only v4 is exercised)
[ ] Periodically re-run `make trace` on kernel/glibc upgrades to catch syscall drift

Medium-term
-----------
[ ] Per-session resource controls documented with concrete examples
[ ] Fragment linter (wraps libnftables dry-run, runs as pre-commit on /etc/authnft/users)
[x] Structured journald fields (AUTHNFT_EVENT=open/close with shared
    AUTHNFT_CORRELATION token; see docs/INTEGRATIONS.txt §6.2)
[ ] Optional audit subsystem hook on fragment rejection
[ ] Pluggable fragment sources (not just /etc/authnft/users/<name>)

Deferred
--------
[ ] Cgroupv1/hybrid support — not planned; document as unsupported
[ ] Non-systemd init — not planned
[ ] Shipping a default fragment set — administrator responsibility

Upstream / ecosystem
--------------------
[ ] Package for Arch (AUR) and Debian
[ ] Submit to distro PAM module lists once past alpha
